{
  "name": "blue-guardians-api",
  "version": "1.0.0",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
  "start": "npm install -g json-server && json-server --watch db.json --port ${PORT:-3000}"
}
}
